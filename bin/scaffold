#!/usr/bin/env node
'use strict';

const { main } = require('../src/main');
const { color } = require('../src/ui');

main().catch((error) => {
  console.error(`${color.red('scaffold:')} ${error.message}`);
  process.exit(1);
});
