{{TAILWIND_IMPORT}}import react from '@vitejs/plugin-react';
import { defineConfig } from 'vite';

export default defineConfig({
  plugins: [react(){{TAILWIND_PLUGIN}}],
  server: {
    port: {{DEV_PORT}},
  },
  preview: {
    port: {{DEV_PORT}},
  },
});
