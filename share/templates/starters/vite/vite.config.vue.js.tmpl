{{TAILWIND_IMPORT}}import vue from '@vitejs/plugin-vue';
import { defineConfig } from 'vite';

export default defineConfig({
  plugins: [vue(){{TAILWIND_PLUGIN}}],
  server: {
    port: {{DEV_PORT}},
  },
  preview: {
    port: {{DEV_PORT}},
  },
});
